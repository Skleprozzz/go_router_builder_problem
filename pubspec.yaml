name: go_router_builder_problem
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # go_router: ^15.2.0
  go_router: 15.1.3
  provider: ^6.1.5
  bloc: ^9.0.0
  flutter_bloc: ^9.1.1
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  # go_router_builder: ^3.0.0
  go_router_builder: 2.9.0
  build_runner: ^2.4.15

flutter:
  uses-material-design: true
